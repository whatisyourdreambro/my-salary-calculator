# 광고 실험 로그 (승인제)

원칙: 실험안 사전 보고 → 운영자 승인 → 적용 → 전/후 14일 비교 → 유지/롤백.
공통 안전장치: 광고 컴포넌트 내부 수정 금지 · 새 UI 광고 위 금지 · share/[data]는
AdSense 1개+쿠팡 1개 상한(2026-08-17 쿠팡 1개 운영자 승인 — 종전 "광고 금지"에서 개정) ·
페이지군별 독립 커밋(revert 가능) · 긴급 롤백은 env 제거 재배포.
★같은 슬롯은 경로당 1회 dedup — PageFooterAds(InArticleAd 포함)가 있는 페이지에
InArticleAd를 또 넣으면 "추가"가 아니라 하단 것이 사라지는 "이동"이 된다 (2026-08-17 확인).

---

## 실험 #1 — Display2Ad 확산 (상태: ✅ 승인·적용 2026-08-17)

**가설**: display-2 슬롯(8284703133)이 홈 1곳에만 배치돼 있어 최저활용 상태. 광고 밀도가 낮고(슬롯 ≤4) 본문이 긴 페이지군에 1개씩 추가하면 기존 슬롯 잠식 없이 페이지 RPM이 오른다.

**적용 대상 (1순위 3개 페이지군, 각각 독립 커밋)**:
1. `/monthly/[amount]` (~105 URL) — "상여금 포함 연봉 환산표" 섹션 직후 (기존 광고와 1스크린+ 간격)
2. `/table/2026/{annual,monthly,weekly,hourly}` 4페이지 — 표 본문과 FAQ 사이 (페이지별 삽입 — layout 삽입은 PageFooterAds와 2연속이라 금지)
3. `/salary-db` 인덱스 — CalcResultAd와 InArticleAd 사이 회사 리스트 중간

**보류(과밀 5슬롯+)**: salary-db/[id]·salary/[amount]·calc/[slug]. **제외**: guides/[slug](6슬롯).

**측정**:
- 적용 전 14일: AdSense 보고서(페이지 분류, URL 필터 /monthly/·/table/2026/·/salary-db) — RPM·노출·클릭 스냅샷
- 적용 후 14일: 동일 지표 + "광고 단위별" 보고서에서 display-2 유닛 단독 수익 + GA4 ad_impression(slot_kind)
- **판정**: 페이지군 RPM +5%↑ AND unfilled 급증 없음 AND 기존 result/fluid 클릭 잠식 없음 → 유지·2순위 확대 / 아니면 해당 커밋 revert

**운영자 결정**: 승인 시 "실험 1호 진행해"라고 말씀해 주세요. 1번(monthly)만 먼저도 가능합니다.

| 항목 | 값 |
|---|---|
| 승인일 | 2026-08-17 (수익 감사 후 운영자 선택 승인) |
| 적용일·커밋 | 2026-08-17 — monthly/table/salary-db 3개 독립 커밋 (git log 참조) |
| 전 14일 RPM | **사이트 전체** 페이지 RPM $3.55 (8/3~8/16, 운영자 제공 일별 CSV 2026-09-05 수령 — 페이지군 필터 불가. AdSense '페이지' 보고서는 최근 30일만 남아 페이지군 소급 불가) |
| 후 14일 RPM | **사이트 전체** $3.50 (8/17~8/30). 노출/PV 4.3→4.3 동일, Active View 60.4%→59.1% |
| 판정 | ★**판정 불가 · 현상 유지 — 확정(2026-09-06)**. 사유: **측정 설계 불성립** — ① 2026-09-06 광고단위 CSV는 **날짜 열이 없는 계정 누적표**(합계 $131.32 = 수동 일별 누적 $130.45 + 약 11.0~11.4시간 잔차)라 전/후 창 자체를 구성할 수 없고(판정식 6개 조건 중 "후 창 단독 수입>0"조차 검증 불가 — $0.32는 누적이지 후 창 값이 아니다) ② 전 창(8/3~8/16)에 유닛이 존재한 기간이 **1.03일**뿐이다(슬롯 활성 f5c1375 = 2026-08-15 23:21:47 KST). **표본 19클릭<50은 부차 사유.** → **28일 연장도 창을 만들지 못하므로 연장 없이 종결 권고(승인 L)**. display-2 누적 실적(참고 — 창 값 아님): $0.32 · 1,545노출 · 19클릭 · CTR 1.230% · CPC $0.01684 · 커버리지 92.39% · ActiveView 37.89%. **현상 유지 = revert 아님**(2026-09-05 운영자 "계획대로 진행" 승인, 10배 계획 §5-4). 판정 불가는 27a692c 병합 게이트만 해제하고 **#2a~c 확장 게이트는 계속 잠금**. 참고(사이트 전체 축, 판정 근거 아님): 전 14일 $3.55 → 후 14일 $3.50, 노출/PV 4.3→4.3, AV 60.4→59.1% — 후 창에 대상군 접촉 커밋(948ce7c·021d335·13b7c62·d7f216e·8d585f5)이 섞여 페이지군 RPM 축은 무효 |

**★기준선 리셋 2026-09-05 (2026-09-06 정정)**: 27a692c(IN_ARTICLE `<ins>` 폭 0px 수정) main 병합·배포.
**병합 전 기준선 = "미상 — 날짜 × 광고단위 보고서 필요"**(종전 "노출 0에 가까움"은 폐기). 종전 서술 "이 슬롯(3302558597)은
2026-05-13 이후 전 사이트에서 광고 요청 자체가 없었다"는 **문자 그대로는 반증됨** — 누적 요청 530 · 매칭 497 · 노출 449 ·
$0.14 · 3클릭 · 커버리지 93.77% · ActiveView 59.60% · 평균 가시 7.88초가 존재한다(수동 총요청 315,120의 0.17%로 사실상 무력화).
이 530건이 (a) 파손창 116일의 잔여 누수인지 (b) 27a692c 전파(9/5 11:45 KST) 이후 약 14.7시간의 회수분인지는
**날짜 열이 없는 누적표로는 갈리지 않는다**. 정상 서빙 퍼널(채움률 93.77%·AV 59.6%·가시 7.88초는 현행 정상 유닛 6.5~12.0초와 같은 대역)과
GuideMid 환산 기대치(GuideMid 145파일 15,990요청/13일 = 51req/h → InArticle 117파일 환산 ≈41req/h × 14.7h ≈ 600) 대비 **88%**라는 규모는 (b)를 지지하나 **추정**이다.
**병합일 이후 구간은 #1 후속(#2a~c)·9/13 홈 결과직하·9/17 전면최적화 판정의 기준선으로 쓸 수 없다**(병합일 경계로 구간 분리).
확정 근거는 **기간 지정 2026-09-06~09-12 광고단위 CSV 1장**(콘솔팩 세션 2, 파일명 `2026-09-13-units-0906_0912.csv`) —
판정선: IN_ARTICLE 주 요청 **≥5,000**(≈노출 4,500) = 완전 회수 / **1,000~5,000** = 부분 회수 / **<1,000** = 미회수·재조사.

적용 메모: env `NEXT_PUBLIC_ADSENSE_SLOT_DISPLAY_2` 미설정 시 전 배치 자동 미렌더 —
CF Pages env 설정 여부를 운영자가 확인해야 실험이 실제 개시됨(홈 display-2가 이미
노출 중이면 설정돼 있는 것). 배치 위치: /monthly 상여금 환산표 직후 · /table 4종
본문과 시즌 링크 사이 · /salary-db 18번째 카드 직후.

---

★**기준선 리셋 2026-09-07 (D0) — 2026-09 전수검사 8커밋 병합·배포**

`claude/website-full-audit-2sktmp` 의 a99c53b~07748da 를 `main` 에 병합(fast-forward, 충돌 0).
게이트 전량 통과 상태에서 배포: vitest 262 · verify:tax/site/companies/sitemap/bonus OK ·
ad-audit ERROR 0(fixed 헤더 가림 후보 4 → **0**) · lint error 0 · qa:crawl 239쪽 전 항목 통과.

**이 배포가 광고 축에 만든 변화**(판정 창을 나눠야 하는 이유):
1. `/job`·`/job/[slug]`·`/industry`·`/industry/[slug]` 91쪽 상단 `HomeTopAd` 가 `pt-header` 로 내려가
   fixed 헤더 뒤 약 23~40px 가림이 해소됐다(top 24px → 96px). = 승인 대기 '⑮ 헤더 가림 4곳' 이 실행된 것.
2. 쿠팡 공정위 고지문이 `PageFooterAds` 소비 41지면에서 새로 렌더되며 **그 아래 `HomeTopAd` 를 25~40px 밀어냈다.**
   고지문은 법적 필수라 되돌릴 수 없다. 무손실 해법은 `PageFooterAds` 순서를
   `HomeTopAd → InArticleAd → CoupangBanner` 로 바꾸는 것이나 **배치 변경이라 승인 항목**이다.
   → **2026-09-11 `f2379fa` 로 이 순서 변경을 배포했다**(운영자 지시 '광고 수익 극대화, AdSense 위치 변경 허용'). 41지면 HomeTopAd 밀림 해소.
3. `/qna` 하단 `InArticleAd`+쿠팡이 죽은 칩("4대보험 & 세금") 세션에서 소멸하던 것이 해소.
   단 가드(`filteredData.length > 0`)는 유지 — 없애면 "검색 결과가 없습니다" 빈 화면 위에 광고가 남는다.
4. `/table` 시급·주급 4쪽 프리렌더 본문 0행 → 100행 (LCP/CLS 미측정 — lighthouse.yml D0 실행 결과로 확인).

**판정 영향**: 홈 description(CTR 실험 문자열)이 이 배포에 섞였으므로 9/13·9/20·9/21 판정과
M01(10/19~26)의 전/후 창은 **D0 경계로 분리**한다. 27a692c(IN_ARTICLE 폭 0px)는 여전히 미병합이며,
L9(samsung-bonus 첫 광고 깊이 상향)는 그 병합이 선행돼야 효과가 있다.

---

## 실험 #2 — Display2 확산 2단계 (상태: 설계 완료, 실험 #1 판정 통과 시 진행)

**전제**: 실험 #1 판정 통과. ★**2026-09-06 현재 이 전제는 충족되지 않았다** — 실험 #1은 '판정 불가·현상 유지'로 확정됐고(측정 설계 불성립), 계획 §8-2에 따라 판정 불가는 확장 게이트를 해제하지 않는다. 따라서 **#2a~c 는 계속 잠금**이며, 잠금 해제는 승인 L 확정 + 새 실험 설계(날짜 지정 광고단위 CSV 로 전/후 창을 만들 수 있는 형태) 이후에만. 판정 전 적용 금지 — display-2 단독
수익 측정을 오염시킴.

**적용 대상 (군별 독립 커밋·14일 비교)**:
- **#2a**: GuideMid 기사용이라 D1에서 수리 못 한 성과급 19곳(기존 12 —
  doosan-enerbility·gs-caltex·hanwha-aerospace·hyundai-mobis·hyundai-rotem·kepco·
  lg-display·s-oil·samsung·samsung-display·sk-hynix·sk-innovation — +
  holiday-bonus·incentive-tax·january-bonus·severance-vs-pension·vacation-pay·
  year-end-bonus·year-end-bonus-tax) + car-loan(466행) — page 중단
  `InArticleAd`→`Display2Ad` 교체. 유닛 순증이 아니라 **dedup으로 죽어 있던
  layout/PageFooterAds 하단 InArticle 부활 + display-2 중단 진입**.
- **#2b**: glossary 인덱스 topAd `HomeTopAd`→`Display2Ad` (footer HomeTop 부활).
- **#2c**: 실험 #1 보류 3군(salary-db/[id]·salary/[amount]·calc/[slug]) — #1
  판정에서 기존 슬롯 잠식 0 확인된 경우에만, 기존 유닛과 1스크린+ 간격 1개씩.
  salary/[amount]는 254행 부근(2026-08-23 죽은 유닛 제거 자리 — 주석 참조).

**판정**: #1과 동일 기준(RPM +5%↑ AND unfilled 급증 없음 AND 잠식 없음), 군별
독립 revert. 커밋 전 `node scripts/ad-audit.mjs --diff` 필수.

---

## 승인 대기 — 2026-09-06 광고단위 CSV 판정 후 상정 (예/아니오 한 줄)

정본: `docs/revenue-100x-plan-2026-09.md` §6(승인 K~N). **코드 선행 금지 · 무응답 = 현행 유지**.

| ID | 요청 | 근거 (1줄) | 하방 (1줄) |
|---|---|---|---|
| **K** | 9/3 전면최적화로 순증한 **멀티플렉스 배치 축소·철회**(목록형 상세 9군 a5b1e26 · 데이터 리포트 3편 e11361f · 연말정산·세금 19곳 f833357) — 예 / 아니오 / **자료 도착 후 재상정(권고)** | 멀티플렉스 1910866475 커버리지 **39.51%** · 미매칭 3,688 = 전체 20,980의 17.58% · 노출RPM $0.107(최하위) · AV 33.59% · CTR 0.129% · 배치 파일 9/3에 13→47(×3.6) | 수익 감소는 $0.03~0.11/일(월 $1~3)로 무시 가능하나 **판정 자료가 없어 지금 철회하면 원인 오귀속으로 인벤토리만 잃는다**(같은 배치가 GuideMid 104→145, 9/5 반등 87.5%가 대안 가설과 정합) |
| **L** | 실험 #1을 **"판정 불가·현상 유지"로 종결**(28일 연장 없이) + 10배 계획 §8-2 "창 내 클릭<50"의 표본 대상을 **실험 유닛(display-2) 기준**으로 확정 — 예 / 아니오 | 날짜 열 부재로 전/후 창 구성 불가 + 전 창 유닛 존재 1.03일. 현재 계획 §8-2와 `scripts/adsense-report.mjs:758`이 서로 다른 표본(19 vs 7,085)을 써 정본이 없다 | **배치 변경 0**(현상 유지 = revert 아님). display-2 확산 유효성을 영구 미판정으로 남기는 것 하나뿐이고 누적 $0.32·AV 37.89%라 미판정 비용이 낮다. 연장하면 실험 슬롯 1칸만 10/4까지 묶인다 |
| **M** | 결과창_본문(CalcResultAd) 직하 유닛 **위치 이동·간격 확대(승인 큐 ⑦) 앞당기기** — **권고: 아니오(보류)** | 우발 클릭 서명 적중은 결과창 단독(CTR 4.147% · CPC $0.01196 = 상단 $0.03193의 37%)이나 4.147%는 약 9.5개월 누적이고 9/1~9/4 수동 CTR은 2.20%로 이미 임계 아래 = 현행 발화 미입증 | 결과창 = 수동 수익 **64.48%** · 사이트 누적 수익 **9.98%**. 한계수익도 비교 유닛에 따라 $0~+$21.5로 갈려 "기대 손실 ≈ 0" 논거 불성립. ★"결과 카드와 광고 사이 분리선/여백" 변형은 **광고 위 UI 삽입 금지**(2026-08-16 수익 급락)에 저촉 → 승인 상정 이전에 자동 기각 |
| **N** | 사장 유닛 **6458241606(구 GUIDE_MID)을 8번째 수동 유닛으로 재사용** — **권고: 아니오(보존만·삭제 금지)** | 이 유닛은 "미사용"이 아니라 2026-05-05~08-24 GUIDE_MID 정본이었고 커버리지 95.50%로 13유닛 중 1위 = 품질 문제 없음. 그러나 노출/PV 5.30·사이트 AV 6월 69.6%→9/4 51.6%로 밀도 레버 소진 | 슬롯 env 접촉은 규칙 1의 예외를 여는 것이고 9/17 전면최적화 D+14·9/21 앵커 ON 두 판정창을 동시에 오염시킨다. 실익 월 $1~3 vs 하방 판정 게이트 2개 무효화 |

★선행 자료: **K** = 9/1~9/6(또는 9/1~9/8) 날짜 × 광고단위 교차 CSV 1장 · **M** = 최근 28일 창 지정 units CSV + AdSense 정책 센터·무효 클릭 알림 이력 캡처(콘솔팩 세션 1 ③ · 세션 2 3).

## 승인 배치 기록 (실험 아닌 단발 승인)

- 2026-09-05 **10배 계획 Phase 1 (운영자 "계획대로 순차적으로 진행")** — 정본 docs/revenue-10x-plan-2026-09.md §3·§5.
  ① 27a692c main 병합·배포(수리 ≠ 실험 — 실험 슬롯 미소비, 기준선 리셋일 = 배포일).
  ② AdPlacement.tsx data-ad-status 관찰자에 ad_filled/ad_unfilled 계측(슬롯당 1회) — 광고 컴포넌트 내부 계측 예외,
  요청·렌더·dedup·접힘 로직 무변경, 독립 커밋·즉시 revert 가능. ③ §12-2 ⑪ 하단 고정 UI 앵커 감지 공유 유틸
  (src/lib/bottomAdDetect.ts): InstallPwaBanner 앵커 감지 시 숨김(fail-closed), BottomSheet 하단 여백, FloatingShareBar 동일 로직 이관.
  토스트 8곳(2.8초 노출)은 미적용 — 후속. 검증 게이트: tsc 0·vitest 131/131·eslint 0·ad-audit --diff ERROR 0/WARN 0.
  실험 판정 규칙 개정(§5-4): 광고단위 1축 우선, 창 내 클릭<50 → 28일 연장, 미달 시 '판정 불가·현상 유지'. ★2026-09-06 단서 추가: **창을 구성할 수 없거나 연장 후 도달 불가가 산술적으로 확정되면 연장 없이 즉시 종결**(10배 계획 §8-2). 표본 대상 정본(실험 유닛 vs 잠식 탐지 표본)은 승인 L 대기.

- 2026-09-05 **보고(승인 아님) — 수동 유닛 커버리지 하락과 미채움 접힘 구조** (100배 계획 F6·adsense-quality-3/4).
  수동 광고단위 커버리지 8월 94.0% → 9/3 85.0 · 9/4 83.9 · 9/5 87.5%, 노출 RPM 0.51→0.42. 코드측 후보: 9/2 전면최적화의 본문 끝 Multiplex(목록형 ≈660라우트) · DART lite 219p CalcResult+GuideMid · /tools 24 GuideMid.
  ★**2026-09-06 정정: 요청/PV 는 감소가 아니라 증가다** — 8월 월간 1.45 → 9/3~9/4 **1.80(+24%)** → 9월 월간 1.69. 종전 "8월 ≈1.46 → 9월 ≈1.33"의 1.33은 어떤 창 조합에서도 재현되지 않는다. 또한 전면최적화 커밋 10건의 git date 는 9/2 가 아니라 전부 **2026-09-03 07:26 KST** 다. 유닛 특정은 부분 완료 — **멀티플렉스 1910866475**(커버리지 39.51% · 미매칭 3,688 = 계정 전체 20,980의 17.58% · 노출RPM $0.107 최하위)가 알려진 유닛 중 유일하게 산술적으로 9/3~9/4 초과 미매칭 1,131건을 설명하며, MultiplexAd 배치 파일이 9/3 배치로 13→47(×3.6) 늘어난 것과 정합한다(사이드바 91.17%는 55,000요청 필요·불가, 가이드중간 95.01%는 커버리지를 올린다). 단 같은 배치가 GuideMid 를 104→145파일로 늘렸고 신규 컨텍스트의 초기 저채움은 생애 누적 커버리지로 분리 불가하므로 **단독 귀속은 확정 불가**(9/5 87.5% 반등이 이 대안 가설과 정합). ★지표 정의: 멀티플렉스가 배치된 이상 총 커버리지는 사이트 건강 지표로 쓸 수 없다 → 게이트에 **"ex-multiplex 커버리지"**(전 기간 94.40% vs 포함 93.34%) 병기. 배치 철회는 **승인 K**(자료 도착 후 재상정 권고).
  구조: AdPlacement 는 minHeight(home-top 120·result 250·guide-mid 250·fluid 200·multiplex 280·sidebar 600)를 예약한 뒤 `data-ad-status=unfilled` 면 컨테이너를 `display:none` 으로 접는다 → 미채움 12~16% 가 예약 높이 붕괴 경로를 타서 모바일 본문 중간 250~280px 붕괴 = CLS 악화 구조(고위험은 본문 중간 GuideMid·결과 직하 CalcResult, Multiplex 는 본문 끝이라 영향 작음, 사이드바는 모바일 무영향).
  **판정**: 9/7 광고단위 CSV 의 커버리지 열(유닛 행, 배치 변경 0건 유닛과 같은 창 비교, 9/2~9/4 vs 9/5~ IN_ARTICLE 첫 요청 구간 분리) + GA4 `ad_unfilled × slot_kind × page_path`(9/7 측정기준 등록 이후 적재분) + GSC 핵심 웹 바이탈(CrUX, 9/13 세션) → 9/17 판정 절에 '철회 후보(유닛×페이지군)'·'CLS 회귀' 열. **철회 후보·래퍼 min-height(공백 감수)·unfilled 시 접힘 대신 높이 유지(AdPlacement 내부)는 전부 승인 항목으로만** — 코드 선행 금지. 배수 계상 없음(판정 인프라).

- 2026-08-17 **/home-loan 본문 중간 GuideMidAd 1개** — CalcResultAd~PageFooterAds
  사이 ~390줄 무광고 구간 해소. InArticleAd는 dedup 함정으로 회피.
- 2026-08-17 **/share/[data] CoupangBanner 1개** — 카톡 공유 랜딩 쿠팡 인벤토리 0
  해소. 기존 CalcResultAd 아래 배치.
- 2026-08-23 **마스터플랜 일괄 승인 배치** (운영자 계획 승인 = 광고 배치 일괄 승인,
  배치별 독립 커밋): D1 dedup 복원 15곳(InArticle→GuideMid)·D3 glossary 부분 복원·
  죽은 유닛 2곳 제거·C1 홈 3→5유닛·C2 성과급 허브 0→2유닛·C3 fun 게임 6종 결과
  한정 InArticle·C4 insights 인덱스 GuideMid·C5 tools/finance/bonus GuideMid.
  검증 게이트: scripts/ad-audit.mjs (전 커밋 ERROR 0 통과).
- 2026-09-02 **전면 최적화 배치 (운영자 지시 "모든 페이지·헤더·광고 수익 최적화 및 초고도화")** —
  190라우트 전수 감사(파인더 11·반박 검증 206·정적 게이트) 후 커버리지 갭에 기존 슬롯만 순증.
  라우트 107곳 고유 AdSense 슬롯 증가(합계 705→821), 감소 0. 핵심: DART 상장사 219p 2→4
  (CalcResult 환산 직하+GuideMid 섹션 경계) · /tools 리프 24종 3→4(ToolPageContent 공용 GuideMid,
  섹션≥2·FAQ 보유 조건) · 신규 /calc 정적 9종 3→4(GuideMid FAQ 경계) · 시즌·세금 단일 페이지 40여 곳
  +1~2(GuideMid/CalcResult/본문 끝 Multiplex) · 목록형 상세(job 62·industry 27·region 19·hub 8·
  qna 59·glossary 58·en/guides 13·salary-db/compare 413·insights 3) 본문 끝 Multiplex 1개 ·
  인덱스 12곳 목록 중간 GuideMid · guides/[slug] 337p·en 가이드 사이드바 300x600 래퍼만 sticky
  (카드 스택 고정 해제 — 뷰포트 초과로 유닛이 잘리던 문제).
  **불변 유지**: 실험 #1 대상(/monthly·/table·/salary-db 인덱스)·홈·Display2 신규 0건, A4 게이트
  (salary-db/[id]·calc/[slug]·salary·monthly·table Multiplex) 0건, 쿠팡 신규 0건, 광고 컴포넌트 내부
  무변경, 기존 광고 이동·삭제 0건. 검증 게이트: ad-audit ERROR 0(INFO 58 기준선 동일, --diff WARN 2건은
  사이드바 래퍼 클래스 변경 오탐)·tsc 0·eslint 0·vitest 124/124. 판정: 배포+14일 섹션별 RPM
  (AdSense 페이지 보고서 — /tools·/salary-db/listed·/calc 신규 9종·시즌 단일 페이지군 비교).
  기각(재제안 금지 사유 기록): HomeTop 직후 Multiplex 연속 배치(minimum-wage-2026/2027·new-employee·
  samsung-negotiation 하단), 레이아웃 쿠팡 위로 UI를 미는 리포트 배선 3건, /company(308 redirect) 광고.
  - ad-audit 신설 INFO 2종 기준선(2026-09-05, 10배 계획 adsense-quality-6·100배 계획 B13): 우발 클릭 인접 후보 **41건**(구현 초안의 42는 오기 — `node scripts/ad-audit.mjs` 실측 41) / fixed 헤더 가림 후보 **4건**(/job·/job/[slug]·/industry·/industry/[slug] HomeTopAd — 헤더 72px fixed 가림). INFO 전용·exit 불변·자동 수정 없음. `--diff` 는 신규 후보만 상세 출력. 후보 수리는 승인 큐(10배 계획 §2-1-b ① 우발 클릭 의심 유닛 간격·위치 수리, 100배 계획 §6-A 헤더 오프셋)로만 — 같은 커밋에서 수정 금지. 배치 후 count 줄이 41/4 를 넘으면 증가분을 이 표에 기록.
    - ★**2026-09-25 증가 기록(ADS-03, 소급)**: 인접 후보 41 → **44**(9/6~9/19 사이 증가, 기록 누락 — 종전 정규식 실측) → **46**(B4 `49d3ac72` 가 트리거에 ResultSharePanel·PrivateFeedback·select·textarea 를 더한 뒤. 통합 브랜치 HEAD 실측도 46, 스크립트가 "확장 전 44건 → 확장 후 46건" 한 줄을 함께 찍는다). 새로 잡힌 2건: `SimpleCalculatorView.tsx` CalcResultAd ↔ ResultSharePanel, `civil-servant-pay-2027/page.tsx` CalcResultAd ↔ PrivateFeedback. 헤더 가림 후보는 4 → **0**(9/7 D0 배포로 해소).
      같은 날 `--diff` 규칙 강화(`49d3ac72`·CI `6a8a332f`): 광고 바로 위 새 UI = ERROR(승인 ID 가 있는 `ad-audit-allow.json` 항목만 면제), 15줄 창 INFO, CI 에서 git diff 실패 시 exit 2. 새 규칙으로 9/8 이후 이력(`--diff --base b757c57a`)을 재생하면 ERROR 4건 — calc/[slug] NextActions → CoupangBanner · civil-servant-pay-2027 CivilPayForecastSelector → HomeTopAd · 같은 쪽 PrivateFeedback → CalcResultAd · year-end-tax-preview ShareSection → CalcResultAd — 과 종전 2줄 창이 놓친 넓은 창 후보(qna → Multiplex, year-end-tax-2027 → Multiplex, samsung-negotiation-2026 → HomeTopAd, home-loan nav → CalcResultAd `3de3d603`, 홈 SalaryCalculator NextActions → ResultAd `4a191ea9`, 홈 월급 시계 → HomeTopAd `981dde76` 등)가 나온다. **이 기록 커밋에서 수리하지 않는다** — 수리는 운영자 9/25 승인 6번(A03·A05·A06)으로 아래 2026-09-25 절 4의 일정에 따른다.

---

## 2026-09-25 — 판정 전제 정정 · 자동광고 복구(P0) · 승인 원장 (감사 B5 · 운영자 "권장하는거 전부 진행")

정본 우선순위: 운영자 9/24 보고서(`docs/revenue-audit-2026-09-24/naver-cloudflare-adsense-report.md`)가 이전 계획과 충돌하면 보고서가 우선한다. 이전 행·값은 지우지 않고 날짜를 붙여 덧붙인다.
수치 출처: AdSense 콘솔 읽기 전용 추출(2026-09-24 KST, USD, 날짜는 서울 기준으로 표시되나 계정 보고 시간대는 운영자 보고서 §9 대로 미확인 — 비교 창은 모두 같은 설정으로 뽑는다). 단순 전후 차이를 인과 효과로 부르지 않는다.

### 1. 자동 인페이지 붕괴(9/10)와 복구 배포(9/24) — 사실

- **원인**: `4eda6889`(2026-09-10 01:39 KST, "unify site UI and streamline initial rendering")가 루트 템플릿 래퍼의 `h-full` 클래스를 없앴다. AdSense 자동광고가 저장해 둔 배치 선택자가 사이트 전체에서 맞지 않게 됐다.
- **관측**(날짜×광고 형식, 자동 인페이지 정의는 2절): 9/9 **2.63**/PV → 9/10 **0.47**/PV. 창 평균 9/1~9/9 **2.99** → 9/10~9/23 **0.72**/PV. 같은 창에서 앵커 0.78 → 0.83/PV, 모바일 전면 0.29 → 0.34/PV 로 거의 그대로다 — 빠진 것은 자동 인페이지뿐. 페이지 RPM $4.09 → $2.97, 일 수익 $14.84 → $9.51. 이 차이에는 시장 단가·PV 구성·간격 실험 절반 배정이 섞여 있으므로 전부를 이 원인에 귀속하지 않는다.
- **복구**: `4d80ce3e`(커밋 9/24 23:05) 루트 래퍼 `h-full` 복원 → Cloudflare Production **2026-09-24 23:25 KST 성공**(운영자 보고서 §1·§3). 재발 방지: `src/lib/__tests__/adCriticalShell.test.ts` 가 루트 셸 클래스를 고정한다 — 개명·삭제 금지.
- ★**2026-09-25 실측 주의 — 캐시된 /salary/* 에는 복구가 아직 없다**: `/salary/50000000` 이 CF 엣지 캐시 HIT(Age 84,561초, Last-Modified 2026-09-23 16:13 GMT)로 서빙되고 HTML 에 `h-full` 이 없다(`class="w-full min-w-0"`). 캐시되지 않는 `/calc/samsung-bonus` 에는 반영됐다. → 운영자 **Purge Everything**(`docs/operator-console-pack.md` 2026-09-25 할 일 0번). /salary 계열은 Purge 시각 이후만 '복구 상태'로 본다.

### 2. P0 — 복구 효과 확인 (사전 등록 · 운영자 보고서 §7 P0)

- **정의 — 자동 인페이지 노출 = '광고 형식' 보고서의 인페이지 행 − 같은 날 수동 광고 단위 노출 합계.** 인페이지 행에는 수동 유닛이 들어 있다(형식 행 합 $323.47 이 사이트 합 $325.23 과 맞으려면 수동 $61.76 이 인페이지 안에 있어야 한다 — 감사 CON-03). 사이트 전체 노출/PV(4.77 등)와 섞지 않는다.
- **자료**: AdSense 날짜×광고 형식(`ag=date,adpresentation`) + 날짜×광고 단위(수동) + 날짜(PV·수익). 완료일만.
- **창**: 첫 완료일 **9/25**(9/24 는 대부분 배포 23:25 이전이라 제외). **7완료일 = 9/25~10/1 → 10/2 조회**, **14완료일 = 9/25~10/8 → 10/9 조회**.

| 기준 창 | 일 수익 | 일 PV | 페이지 RPM | 자동 인페이지/PV | 수동/PV | 앵커/PV | 전면/PV |
|---|---|---|---|---|---|---|---|
| 9/1~9/9 (붕괴 전) | $14.84 | 3,628 | $4.09 | **2.99** | 1.53 | 0.78 | 0.29 |
| 9/10~9/23 (붕괴) | $9.51 | 3,200 | $2.97 | **0.72** | 1.91 | 0.83 | 0.34 |
| 9/17~9/23 (복구 직전 7일) | $9.61 | 3,013 | $3.19 | 0.72 | 1.93 | 0.85 | 0.32 |
| 8/27~9/23 (28일) | $11.62 | 3,345 | $3.47 | 1.65 | 1.67 | 0.80 | 0.31 |

- **판정선(권고, 사전 등록)**: 자동 인페이지/PV **2.4 이상**(붕괴 전 2.99 의 80%) = 회복 · **1.2~2.4** = 부분 회복(캐시 잔존·페이지군별 확인) · **1.2 미만** = 미회복 → 운영 HTML 루트 셸 클래스·CF 캐시 잔존·자동광고 설정 화면부터 다시 본다. 앵커/PV·전면/PV 가 위 범위(0.78~0.85 / 0.29~0.35) 안이면 트래픽 구성 변화가 아니다. 페이지 RPM·일 수익은 같이 적되 판정 축이 아니다(시장 단가·PV 구성·시즌).
- **창 안의 공변량**(날짜를 적는다): 감사 통합 브랜치 `claude/audit-integration-20260925` 배포일 · 9/26 시즌 세트 푸시 · 각 Purge 시각 · CF 캐시 규칙 B/C 생성 시각(C01).
- **규칙: 14완료일 조회(10/9) 전에는 광고량·위치 변경을 겹치지 않는다**(운영자 보고서 §7). 10/6~10/9 에는 광고 수·위치·자동광고 게재 범위를 바꾸지 않는 승인 항목만(4절 10번 A14).

| 조회일 | 창 | 자동 인페이지/PV | 수동/PV | 페이지 RPM | 일 수익 | 판정 | 메모 |
|---|---|---|---|---|---|---|---|
| 10/2 | 9/25~10/1 (7완료일) | | | | | | |
| 10/9 | 9/25~10/8 (14완료일) | | | | | | |

### 3. 판정 전제 정정 (감사 B5)

**(a) 인페이지 최소간격 실험 `8972529716813823` — 종료.** 9/8~9/19, 사용자 50% 배정. 기존 설정(최소 간격 50px, 페이지당 최대 16) 우세 확률 99%. 수입 원본 $42.09 vs 대안(200px) $40.97(−3%), 실험 화면 추정 월 −$6.38(실제 월 손실액 아님), 총 노출 15.3만. **기존 설정 유지·자동 적용 사용 안 함. 200px 은 적용하지 않는다.** 9/24 D+14·10/8 D+28 판정은 소멸(`docs/next-upgrade-plan-2026-09-11.md` §1 정정). 주의: 9/8~9/19 동안 사용자 절반이 200px 이었으므로 9/17 D+14 창(9/3~9/10)과 10/10 D+28 창(9/12~10/9)의 인페이지 지표는 일부 실험 영향을 받는다. 9/10 붕괴는 이 실험으로 설명되지 않는다(9/8~9/9 하락 없음, 9/19 종료 뒤 회복 없음). 이 결과를 복구 후 모든 화면의 영구 최적값으로 넓혀 읽지 않는다(운영자 보고서 §7).

**(b) 앵커 9/21→10/5 판정 — 처치가 없어 취소.** AdSense 광고 형식 보고서(`ag=adpresentation`) 기준 앵커·사이드레일·모바일 전면은 **9/1 이전부터 게재 중**이었다: 9/1~9/9 앵커 25,431노출($42.92, 0.78/PV) · 9/14~9/20 0.84/PV · 9/21~9/23 0.85/PV. 사이드레일은 9/21 뒤에도 3,002노출(9/21~9/23)이 계속됐다. 9/8 라이브 감사도 '오버레이 형식 3/3 활성'이었다(`docs/revenue-audit-2026-09-08/live-adsense-audit.md`). 광고 단위 목록 15개 중 인피드 0개 = 인피드 미발급, 최적화 > 실험에 앵커 실험 없음. → **10/5 는 판정이 아니라 28일 광고 형식 스냅샷**(콘솔팩 세션 4 ③). 원안의 '사이드레일 OFF'(28일 약 $10.5, 수익 3.2% 순감 — 측정된 이득 없음)는 실행하지 않는다 = 운영자 9/25 승인 12번 '현행 유지'. 새 오버레이 변경(앵커·사이드레일·동적 앵커·인피드)은 별도 승인, 10/10 판정 조회 이후. 참고: 페이지 RPM 상승(9/14~9/20 $2.76 → 9/21~9/23 $3.54)은 전 형식 노출 RPM 이 함께 오른 시장 변동이다(인페이지 $0.38 → 0.49 · 전면 $1.78 → 2.12 · 앵커 $1.14 → 1.62).

**(c) 교란 요인(공변량) 기록** — 판정 방식을 바꾸는 결정은 운영자 몫이다. 여기에는 사실과 제안만 적는다.

| 날짜(KST) | 커밋·사건 | 걸리는 판정 | 제안 |
|---|---|---|---|
| 9/9 00:44~19:17 | `00780026`·`4a191ea9`·`6fb66b46`·`735965c8` 광고 위 UI·공유 버튼 | — | 9/11 ad-depth 기준선(`docs/ad-depth/2026-09-11-*.txt`)에 이미 포함 — 10/10 창의 교란 아님 |
| 9/10 01:39 → 9/24 23:25 | `4eda6889` 자동 인페이지 붕괴 → `4d80ce3e` 복구 | 10/10 D+28(9/12~10/9) 전 구간 | 창을 9/12~9/24(붕괴)·9/25~10/9(복구)로 나눈다. 페이지 RPM·URL 채널 RPM 은 판정 축에서 빼고 수동 광고단위 행(CALC_RESULT·HOME_TOP·DISPLAY_2)의 CTR·CPC·커버리지만 본다 |
| 9/19 19:23 | `1833d49c` AdPlacement 라벨 line-height | 10/10 | 기존 각주 유지(`docs/growth-masterplan-2026-09-20.md` §0-α) — 중복 기록 안 함 |
| 9/19 20:13 | `981dde76` 홈 '월급 시계'가 HomeTop 위로 → 홈 첫 광고 하강 | 10/10 HOME_TOP·(취소된) 10/5 | '/' 를 따로 집계 — GA4 `ad_filled`·`ad_unit_click` × page_path 로 홈/비홈 분리 |
| 9/19 22:32 | `3de3d603` /home-loan nav·H2, hyundai title·description | 10/5 S2-5·10/17 | 기존 각주(S2-5 '참고' 강등, hyundai 10/17) 유지 |
| 9/23 종일 → 9/24 00:07 | Workers CPU 한도 1102 장애(9/23 요청 21,700 중 425 kill) → `e801fd1c` 1차 대응 | 9/23~9/24 를 포함한 모든 창 | 장애 공변량 — 그 이틀을 빼거나 PV 당 지표로만 비교 |
| 9/25 이후(기입) | 감사 통합 배포 · 9/26 시즌 푸시 | P0·10/10 | 배포일·Purge 시각 기입. 통합 배포는 광고 컴포넌트 무접촉(`ad-audit --diff --base 4d80ce3e` ERROR 0/WARN 0)이나 /calc/[slug] 일부 본문·계산값(A18·B11)이 바뀐다 — 제목·설명은 불변 |

**(d) ad-audit 인접 후보 41 → 44 → 46** — 위 2026-09-02 배치 기록 아래 9/25 하위 항목.

**(e) 승인 M 재상정 근거 (감사 CON-04 — 증거만)**. 수동 단위 CTR, 9/1~9/9 → 9/14~9/23: 결과창_본문 3.51% → **7.16%**(1,618클릭·$9.01·CPC 약 $0.0056) · 상단 2.09% → **5.78%**(CPC 약 $0.0075) · 가이드중간 3.58% → **5.48%** · 인아티클_0513 3.26% → 4.29% · 수동 합계 2.96% → 5.54%. 상승은 9/10 이 아니라 **9/5 부터**(9/1~9/4 2.20% → 9/5~9/8 3.44% → 9/9 4.01%) — `27bce038`·`4eda6889`·`f2379fad` 보다 앞서므로 9/5 배포(27a692c 병합 등)도 후보다. 최근은 완화 중(9/21~9/23 결과창 5.32%·수동 4.32%). 문서의 우발 클릭 서명(CTR ≥3.5% AND CPC ≤$0.015)을 세 단위가 충족하므로 M 보류 근거('9/1~9/4 수동 CTR 2.20%')는 무너졌다. 정책 센터는 9/24 '현재 정책 위반 문제 없음'. 자동 인페이지 붕괴(9/10~9/24)와 겹친 구성 효과일 수 있으므로 원인을 결과창 단독으로 단정하지 않는다. **M 은 여전히 승인 항목**이며 운영자 9/25 승인 5번으로 실행이 결정됐다 — 시점·변형은 4절.

### 4. 승인 원장 — 광고 (운영자 2026-09-25 "권장하는거 전부 진행", 9/24 제시 30문항 중 광고분)

비광고 문항은 `docs/next-upgrade-plan-2026-09-11.md` §9. 공통 조건: 광고 컴포넌트·슬롯 ID·env·쿠팡/LinkPrice 파라미터는 각 항목이 명시한 범위 밖에서 무접촉 · 새 UI 는 광고 아래 · 위치 변경은 `npm run ad-depth` 전후 JSON 을 `docs/ad-depth/` 에 · `ad-audit --diff` ERROR 0 / WARN 0 · **광고 변경은 겹치지 않는다(운영자 보고서 §7) — P0 확인 뒤 한 주에 한 묶음**(페이지군이 겹치지 않는 22번 단일 실험만 예외) · 11/1 동결 뒤에는 원위치(롤백)만.

| 번호 | 운영자 질문(요지) | 감사 ID | 상태 | 시점·조건 | 메모 |
|---|---|---|---|---|---|
| 5 | 결과창 광고 주변 오클릭 대응 | A02(=승인 M, CON-04)·A04(ADS-02)·A08(ADS-08) | 승인 → 예정 | **10/11~10/17 주**(10/10 판정 조회 뒤), P0 14완료일 회복 전제 | 허용 변형: 유닛 이동 또는 광고 아래·주변 간격 확대(유닛 수 불변). '결과 카드와 광고 사이 분리선·여백'은 광고 위 UI 라 자동 기각. A04 홈 비교기 버튼은 결과 광고 아래로. A08 은 GA4 `ad_unit_click` × page_path 분해(셀당 50클릭 이상) 먼저. 판정 = 7일 뒤 결과창 CTR·클릭당 수익 — 클릭 수 감소를 실패로 읽지 않는다. 정책 통지가 오면 날짜와 무관하게 긴급 예외(C19) |
| 6 | 광고 위로 끼어든 UI 를 광고 아래로 | A03(ADS-01 홈 월급 시계)·A05(ADS-05 공유 버튼 3곳)·A06(ADS-06 공무원 2027 선택기·피드백, 삼성전자 피드백) | 승인 → 예정 | **10/18~10/24 주** | A05 의 HomeTop–Multiplex 사이 8곳은 결정 K 와 함께(공유만 옮기면 기각된 'HomeTop 직후 Multiplex'가 된다). A06 공무원 2027 은 B20 제목 판정(`docs/gsc-sniping-log.md` Round 3, T0+28)과 같은 페이지 — 이동일을 그 행에 각주 |
| 7 | 광고 배치 정리 | A07(ADS-04 /salary-db 목록)·A09(ADS-09 인덱스 끝 연속 → 결정 K)·A10(ADS-10 게임 광고 상시 마운트) | 승인 → 예정 | **10/25~10/31 주** | A07 은 L06' 인피드와 묶음(인피드 유닛 미발급 — 콘솔). A10 은 `ad_request_attempt`/PV 부터 측정. 10/31 을 넘기면 2027-02 |
| 8 | 404·오류 화면 자동광고 멈춤 | A12(ADS-12) | 승인 → 예정 | **10/10 이후**(P0 14완료일 조회 뒤 — 자동광고 정지 범위를 넓히는 변경이라 선택자 실수가 P0 측정을 오염시킬 수 있다) | 대시보드 제외. 수익 영향 거의 0, 정책 위험 제거. 배포 뒤 정상 페이지에서 `pauseAdRequests` 가 걸리지 않는지 확인 |
| 9 | 광고 자리 높이 예약(화면 흔들림) | A36(PERF-04·05) | 승인 → 예정 | **/salary 10/10 이후**(P0 14완료일 조회 뒤) · **회사·보너스 10/18~10/24 주**(6번과 같은 주, 다른 커밋) · samsung-bonus 제외 | 입력 목록 = 5절(9/25 모바일 CLS 실측). AdPlacement `minHeight` 재조정(ADS-05·next-upgrade S3-3)은 보호 코드라 별도 승인, 기본 2027-02 |
| 10 | 홈 계산기 오류 방어 + 하단 공유바 버그 | A14(CLIENT-01 CalculatorTabs)·A15(CLIENT-04·09) | 승인 → 예정 | **A14 10/6 이후**(DOM·광고 위치 불변, 홈은 10/10 판정 라우트 아님) · **A15 10/11 이후**(/calc/samsung-bonus 가 10/10 판정 URL 채널이라) | 홈 섬 3곳(월급 시계·DeferredHomeCalculator·로드맵 차트)은 `3b222f88`·`7bb1ec96` 에서 경계 적용 완료. 남은 것은 ResultAd 를 품은 CalculatorTabs(A14)와 FloatingShareBar ref 이관·인터벌 정리(A15) |
| 11 | /public-institutions 광고 추가 | A11(ADS-11) | 조건부 | 10/5 세션 4 에서 GA4 랜딩 세션 확인 → 예면 10/11 이후, 다른 묶음과 다른 날 | 결과 광고·페이지 끝 광고 각 1(새 UI 는 광고 아래) |
| 12 | 애드센스 오버레이 설정 변경 | CON-01·C13 | **현행 유지(변경 없음)** | — | 3절 (b). 운영자는 '저장' 없이 오버레이 형식 화면 캡처만 |
| 22 | 연말정산 입력칸을 광고 위로(한 페이지 실험) | A32(G5) | 승인 → 예정 | **10/11 이후 시작 가능한 단일 실험** — 5번과 겹치지 않는 1쪽, 동시 실험 1개 | 판정 = AdSense URL 채널 RPM + GA4 `calc_start`·`calc_success` 28일. 시작+14일 중간 확인이 나쁘면 10/31 전 원위치 — 11/1 뒤에는 원위치만 허용 |

### 5. A36 입력 목록 — 모바일 CLS 실측 (2026-09-25, 광고 차단 · 비광고 CLS 만)

측정: 운영 사이트, headless Chrome 153(playwright-core), Android UA·390×844 모바일 에뮬레이션, 19회 로드(10초 기록, 스로틀 시 14초 → 600px 씩 끝까지 스크롤 → 2.5초). 광고·분석 요청(googlesyndication·doubleclick·adservice·googletagmanager·google-analytics·쿠팡·`/adgk/*`)을 차단해 무효 트래픽을 만들지 않았다. 한계: ① 모바일 에뮬레이션의 로드 직후 resize 로 첫 500ms 이동이 `hadRecentInput=true` 로 표시되는 인공물(비에뮬레이션 대조에서 같은 값 확인 — 아래는 원값 합계) ② 측정 PC 의 폴백 글꼴은 맑은 고딕, Android 는 Noto Sans CJK 라 리플로 폭이 다를 수 있다. 원자료는 세션 scratchpad `cls/`(저장소 밖).

| 페이지 | 빠름 | 느림(1.6Mbps·150ms·CPU 4×) | 웹폰트 차단 | 스크롤 중 |
|---|---|---|---|---|
| /calc/samsung-bonus | 0.259(3회 중 2회, 1회 0.009) | 0.239 | 0.005 | 0 |
| /civil-servant-pay-2027 | 0.235 | 0.225 | 0 | 0 |
| /salary/50000000 | 0.141 | 0.135(폰트 0.104 + 버튼 0.031) | 0.031 | 0 |
| /salary-db/samsung-electronics | 0.047 | 0.023 | — | 0 |
| / | 0.033 | 0.031 | — | 0 |
| /teacher-pay-2026 | 0.014 | 0.012 | — | 0 |

두 비광고 원인(웹폰트 swap 9/10·하이드레이션 뒤 공유/즐겨찾기 버튼 9/9)이 GSC 모바일 상태 전환(9/17)보다 약 1주 앞선다 — 추정이며 입증 아님. 실사용자 확인: GA4 탐색 `web_vitals`(CLS) × `cls_target`, 모바일(20% 샘플, 9/14~).

1. **웹폰트 swap 리플로(주원인)** — `src/app/fonts/siteFonts.generated.ts`(생성기 `src/app/fonts/build-subsets.py`)의 서브셋 6개가 `display:"swap"`·`adjustFontFallback:false`, 폴백 목록은 `globals.css` `--font-site-sans`. `97f4a616`(9/10 22:51)이 optional → swap 으로 바꿨다. 느린 로드에서 4~6초 뒤 한글 제목·히어로 줄 수가 줄며 아래가 40~63px 올라간다(samsung-bonus h1 120 → 80px, 계산기 블록 63px 상승). KoreanExtra1(공무원·/salary·회사·교사)과 KoreanExtra4(홈)는 프리로드 없이 요청된다.
   - (a) 크기 보정 로컬 폴백 @font-face(Noto Sans CJK KR·Apple SD Gothic Neo·Malgun Gothic) 및/또는 KoreanExtra1 프리로드 — 최종 레이아웃이 같아 광고 위 높이 불변, DOM 불변이라 P0 와도 무관하다. Android·iOS 실기 시각 확인 후 진행. 원본 전체 폰트로 되돌리기는 금지(LCP 사건 교훈).
   - (b) `display:"optional"` 복귀 — 느린 첫 방문은 더 큰 폴백으로 남아 광고 위 높이가 달라진다(samsung-bonus 전체 30,067 vs 28,584px, 히어로 573 vs 510px) → A36 일정을 따른다.
2. **하이드레이션 뒤에만 나타나는 공유·즐겨찾기 버튼**(0 → 42/44px) — `useSharePageContext` 가 SSR·첫 렌더에서 컨트롤을 생략한다(`6fb66b46`·`735965c8`, 9/9). 서버에서 같은 크기 빈 자리 예약(래퍼 `min-h-[42px]`, 공유 행 `min-h-11`) — 하이드레이션 뒤 광고 위 높이는 지금과 같고 하이드레이션 전 HTML 만 바뀐다(광고 요청은 하이드레이션 뒤).
   - `/salary/[amount]` `page.tsx` FavoritesButton 래퍼 `div.self-center.mb-6` — CLS 0.031, 첫 화면 결과 카드·인아티클 광고를 42px 밀어냄 → **10/10 이후**
   - 회사 `CompanyDetailClient.tsx` ShareButtons 0→44 + FavoritesButton 0→42 = 첫 광고(y≈2144) 위 +86px, CLS 0.023 → **10/18 주**
   - 성과급 계산기 히어로 `div.mt-4.flex.justify-center` 0→42px(samsung-bonus 는 첫 광고 y≈3457 위, CLS 0.009) — samsung-bonus 제외, 나머지 보너스 계산기 → **10/18 주**(FavoritesButton 을 쓰는 파일 41개)
   - 버튼을 없애는 안은 광고 위 42px 감소라 별도 승인.
3. **/salary 차트 자리표시 400px vs 실제 694~931px**(A36 원안 PERF-04) — 2의 /salary 와 같은 날.
4. **홈 계산기 패널 `#calc-panel-salary` 0 → 990px**(하이드레이션, y≈2346, 느린 로드 6.9초) — 하이드레이션 전에 그 위치까지 스크롤한 방문만 CLS 로 잡힌다. Display2 가 2362 → 3376 으로 이동. `src/components/CalculatorTabs.tsx`(useSearchParams + 동적 SalaryCalculator), `src/app/HomeClient.tsx`. A36 일정 밖이고 Display2 위라 **별도 승인, 낮은 우선순위**.
5. **채워진 광고가 예약 높이보다 큼(광고 쪽 주원인 추정)** — 예약: HomeTop 120 · 결과/가이드중간/디스플레이2 250 · 인아티클 220 · 멀티플렉스 280. 28일 소재 크기에 336x280(56,910노출 — 250 슬롯 +30px, HomeTop +160px)·320x320 이 있고 멀티플렉스는 보통 500px 이상. 예: teacher-pay HomeTop(y=502)이 280px 로 채워지면 약 0.045. 수정은 AdPlacement `minHeight`(보호 코드) = ADS-05·S3-3 재조정 승인 항목. 먼저 이미 보내는 `ad_height` 를 GA4 맞춤 측정항목으로 등록해 분포를 본다(콘솔).
- 영향 없음 확인: 헤더 테마 토글(44×44 예약) · 560px 자리표시 하단 섹션(화면 도달 전 교체, 스크롤 CLS 0) · 미채움 슬롯 접힘(모바일 미채움 1.5% = 588/40,242, 화면 밖 제거는 스크롤 앵커링이 보정) · 레이아웃 중복 블록 제거(y≈20.9k·22.7k, 화면 밖).
- 자동 인페이지는 9/10~9/24 대부분 게재되지 않았으므로 9/17 상태 전환의 원인이 아니다. 9/24 복구 뒤에는 자동광고 삽입 이동이 새로 더해질 수 있다 — P0 기간에 GSC·CF 모바일 CLS 를 함께 본다(운영자 보고서 §6: CLS 좋음 89%·개선 필요 6%·나쁨 5%, 9/23~9/24 24시간).
