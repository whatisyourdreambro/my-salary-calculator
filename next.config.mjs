/** @type {import('next').NextConfig} */
const nextConfig = {
  // 👇 이 부분을 추가해주세요.
  output: "export",
  // 👆 이 부분을 추가해주세요.

  // 향후 필요하다면 여기에 다른 Next.js 설정을 추가할 수 있습니다.
};

export default nextConfig;
